{% extends 'base.html.twig' %}

{% block body %}
    <a href="{{ path("request.list") }}" class="btn btn-secondary">К списку</a>
    <h1>Запрос: {{ request.title }}<a href="{{  path('request.edit', {'id': request.id}) }}" class="btn btn-primary">Редактировать</a></h1>
    <div>
        <table>
            <tbody>

                <tr>
                    <td>Сообщение</td>
                    <td>{{ request.message }}</a></td>
                </tr>
                <tr>
                    <td>Статус</td>
                    <td>{{ request.status}}</td>
                </tr>
                <tr>
                    <td>Дата создания</td>
                    <td>{{ request.createAt.format('Y.m.d H:i:s') }}</td>
                </tr>
            </tbody>
        </table>
    </div>
{% endblock %}